#include<stdio.h>
int main (){
    int address=0,i,avg,sum;
    int value[10];
    for(int i=1;i<10;i++){
        printf("enter the value");
        scanf("%d",&value[i]);
    
    
         address = sum+value[i];
         avg= address/10;
           printf("the sum of numbers is %d and avg of a number is %d " ,address,avg);
           return 0;
        }
}

    
    
    
    

